# 操作手順書ジェネレーター — Chrome 拡張機能

画面操作（クリック・入力・ドロップダウン選択）を自動キャプチャし、
スクリーンショット付きの手順書を **Markdown または HTML** で出力します。

---

## インストール方法

1. このフォルダ（`screen-recorder-extension`）をPC上の任意の場所に配置します。
2. Chrome のアドレスバーに `chrome://extensions` と入力して開きます。
3. 右上の「**デベロッパーモード**」トグルをオンにします。
4. 「**パッケージ化されていない拡張機能を読み込む**」をクリックします。
5. このフォルダを選択します。
6. ツールバーに 📋 アイコンが表示されれば完了です。

---

## 使い方

| 手順 | 操作 |
|------|------|
| 1 | ツールバーの 📋 アイコンをクリックしてポップアップを開く |
| 2 | **「記録を開始する」** をクリック |
| 3 | キャプチャしたいページを通常通り操作する |
| 4 | 操作が完了したら 📋 アイコンをクリックして **「記録を停止する」** をクリック |
| 5 | 出力形式（Markdown / HTML）を選択して **「手順書をエクスポート」** をクリック |

---

## キャプチャ対象の操作

- **クリック** — リンク、ボタン、クリック可能な要素
- **テキスト入力** — input フィールド、テキストエリア（0.8秒の無入力後に記録）
- **ドロップダウン選択** — `<select>` 要素

---

## ファイル構成

```
screen-recorder-extension/
├── manifest.json      # 拡張機能の設定
├── background.js      # Service Worker（スクリーンショット取得・状態管理）
├── content.js         # ページに注入されるキャプチャスクリプト
├── popup.html         # ポップアップUI
├── popup.js           # ポップアップのロジックとエクスポート処理
└── icons/             # 拡張機能アイコン
```

---

## 注意事項

- `chrome://` ページや Chrome Web Store などの内部ページはキャプチャできません。
- スクリーンショットは **Base64** 形式でエクスポートファイルに埋め込まれます。
- パスワードフィールドの入力値は記録されません（`type="password"` は除外推奨）。
